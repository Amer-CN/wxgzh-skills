# OBS-360..365 —— 77X 杂项收口六件验收报告（字节级落盘）

- 档号：77X（杂项收口：77V/77W 遗留六件）。
- 授权：RELOCK_ALLOWED 临时 0→1（批准人=用户，范围=wxgzh-pipeline 为主；input_contract 实测在 media 锁条目内→含 media）；GZH_DESIGN_WRITE_ALLOWED 维持 0。授权登记行随 feat 落账（77U-F 新规第二次执行）。
- feat 提交：b156dc1（21 文件：19 改+2 新增）。

## 1. 六件逐项（任务 0 坐实→修复）

| OBS | 遗留 | 坐实 | 修复 |
|---|---|---|---|
| 360 | version_check 无测试豁免门，20 处编排器测试真实 ls-remote（77V 报 19，实测 20——任务 0 更正），未来 pipeline-only 新 tag 超基线日期时集体 STALE 自伤 | `_version_check_step`（orchestrator.py:187）无 env 门 | `WXGZH_SKIP_VERSION_CHECK=1` 豁免门（返回 skipped 留痕）+ tests/conftest.py setdefault；测试 +2（门零 subprocess/env 未设 behind 照旧） |
| 361 | run() doctor 失败分支引用未定义 st（:239-241），doctor 失败路径 NameError 潜伏 | `_wall_seconds` 强依赖 PipelineState.started_at，doctor 失败时 state 尚不存在 | 该分支删 `run_wall_seconds` 键（resume() 同分支本就不带，最小改动口径）；测试 +1（mock doctor 失败→FAIL_CLOSED 不 NameError） |
| 362 | README 表格 pipeline 单元格不在升版同步集，与速览行脱节 | 表格 9R25 vs 速览 9R26（77W/77X 两档未跟） | 单元格→9R28+速览行→9R28/dev32+media 表行 dev32；**README 版本表+速览行自 77X 起纳入每档升版同步清单**（口径 90 成规） |
| 363 | producers `_STABLE_SINGLE_ASSET_FIELDS` 白名单挑选（:845）丢 basis，media_request 层断点——77W 枚举链闭环缺口 | schema 有 basis/recorder 读它/组装层丢弃 | :845 挑选后条件附加 basis（`if rec.get("basis")`）；:530/:533 必填集与 :1441 在位校验不动（basis 非必填，入必填集会误伤 user 车道）；测试 +2（auto_rule+basis 保留/user 无 basis 不携带不报错） |
| 364 | input_contract 交叉校验层无 supplemental 分流，与 validate_media_manifest 双校验器不一致 | input_contract 在 media 锁条目内（runtime_file_count=59），交叉层（Step 3a-3e/5/6/7）零 permalink 逻辑 | Step 3f 追加分流（supplemental 非 null 须 aihot 前缀，构造外站填充拒，口径同 77W REQUEST_MATERIAL_PERMALINK_LANE；normal 不新增门槛）；测试 +2（null 过/外站拒 error 含 77X/OBS-364） |
| 365 | pipeline release_date 2026-07-30 陈旧（77S 起未维护） | VERSION:4 实测 | 9R27→9R28 升版时同步 2026-09-03（写入日口径） |

## 2. 裁决记录（执行中两轮）

1. **test_hf77v 三条编排器级测试转红**（豁免门 conftest 使 mock 流程不再走到）：方案①授权——三条测试开头补 `monkeypatch.delenv("WXGZH_SKIP_VERSION_CHECK", raising=False)` 恢复 mock 原语义（与 77W 夹具对齐同模式）。
2. **README 速览行纳入同步**：速览行与表格同根因，规格 C 立规本体包含它——速览行 9R28/dev32+media 表行 dev32。

## 3. 测试实测

- pipeline 全套：**8 failed, 575 passed, 22 skipped**——7 项基线环境红（hotfix1×1/hotfix7×3/obs171×1/obs80×2）+1 obs304 计数钉子（已随档 241→247 转绿，主智能体）；**零额外新红**；test_hf77v 9 条恢复全绿+test_hf77x_misc 5 绿。
- media 全套：**356 passed, 0 failed**, 7 skipped（含新增 2 绿+dev32 钉子）。

## 4. relock 与锁链

- media relock #105：dry-run 远端见证 PASS（root/entrypoint? no—entrypoint 不变/validator 不变/full_commit/source_tree/version CHANGED dev31→dev32）→ apply 全链 PASS（doctor gate/备份 skills.lock.20260903T100508Z.json/entry `relock-media-enrichment-20260903T100508Z-d72d7213`/installer/post doctor/entrypoint smoke）。
- 新锁 sha：`9e45a0eadc159e4e2b2bb301a89e8fd5547aeacda75f30e20b6700cb6f7d958e`（旧 aa821744…）；R93 双侧同步。
- pipeline 无锁条目：VERSION 9R28+触及面（orchestrator/producers/conftest/测试×2/ledger）cp 同步装机侧。
- doctor：源码侧 PASS（OBS_69/OBS_68 MATCH）；装机侧 PASS（OBS_69/OBS_68 MATCH）。
- upgrade_regression：唯一红 obs154×1（既有在册）；relock dry-run ×4「无变化 OK」。

## 5. 程序校验

- 唯一编号：`247 119 365 True`
- R59：`main=21 partition_active=21` 双差集空
- 授权登记行：恰 1 行；release_date=2026-09-03（VERSION:4）

## 6. 基线链

`5f41603（77W-F）→ b156dc1（feat 77X，含授权登记行+obs304 随档 247）→ <docs 本报告+锁链>`
