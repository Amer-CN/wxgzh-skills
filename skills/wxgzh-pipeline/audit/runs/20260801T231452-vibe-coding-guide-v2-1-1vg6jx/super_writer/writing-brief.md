# Writing Brief

article_mode: medium
length_mode: medium
target_visible_chars: 3850
acceptable_min: 2500
acceptable_max: 4000
output_mode: full

选题：vibe-coding-guide v2.1 升级——第一版文章承诺的「安全气囊」这次真的装上了。素材为用户直接提供（跳过 AI HOT），共 18 条。主线：认账 v1.0 拦不住 → 一天四版 → 体检脚本+强制护栏 → 测试会骗人/双向纠错 → 边界与诚实（主动缩小宣传）。风格沿用第一版公众号文章（短段、口语、自嘲、第一人称）。禁止新增素材包之外的事实；实测只写 Windows + Claude Code；必须写失效场景（无 bash 静默失效 + install.sh 路径不保证生效）。新增四条素材：主动在安装脚本输出声明护栏不保证生效（放弃宣传空间）；AI 用更弱证据推翻自己正确的 hooks 怀疑；277 告警 261 条 CRLF 假告警；README 宣传语比代码落后两个版本。
